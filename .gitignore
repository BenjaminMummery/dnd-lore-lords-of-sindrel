# Allow this repo's pre-commit config (ignored by global ~/.gitignore)
!.pre-commit-config.yaml

# Local secrets and Python tooling
.env
.venv/
__pycache__/
*.py[cod]

# macOS
.DS_Store
**/.DS_Store

# Session transcript working audio (large; keep locally)
notes/transcripts/**/*.wav
